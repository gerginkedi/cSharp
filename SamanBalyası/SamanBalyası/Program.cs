List<int> sortedNumbers = new List<int>();
int choosingNumber;
int check;
int comprassionNumb;
List<int> numbers = new List<int>();
numbers.Add(22);
numbers.Add(27);
numbers.Add(16);
numbers.Add(2);
numbers.Add(18);
numbers.Add(6);


for(choosingNumber = 1; choosingNumber < numbers.Count; choosingNumber++)
{

    for(comprassionNumb = 0; comprassionNumb < numbers.Count; comprassionNumb++)
    {

    }
    if (numbers[choosingNumber]>)
    {

    }

    
}























//sortedNumbers.Add(numbers[0]);
//for (int indexNumber = 1; indexNumber < numbers.Count; indexNumber++)
//{
//    int checkingValue = numbers[indexNumber];
//    int checkingIndex = indexNumber - 1;
//    while (checkingIndex >= 0 && checkingIndex < sortedNumbers.Count)
//    {
//        if (checkingValue >= sortedNumbers[checkingIndex])
//        {
//            sortedNumbers.Insert(checkingIndex, checkingValue);
//            break;
//        }
//        if (checkingIndex == 0)
//            sortedNumbers.Insert(checkingIndex, checkingValue);
//        checkingIndex--;
//    }
//}
//for (int fflai4 = 0; fflai4 < sortedNumbers.Count; fflai4++)
//{
//    Console.WriteLine(sortedNumbers[fflai4]);
//}

//return;

//for (int fflai = 0; 0<numbers.Count; fflai++)//dizi sayısı kadar döndürmek için
//{
//    while(fflai == numbers.Count) //durdurmak için
//    {
//        for(int fflai2 = 0; 0 < numbers.Count; fflai2++) {//eleman seçmek için
        
//            for(int fflai3 = 0; 0 < numbers.Count; fflai3++)//dizideki eleamnları döndürüp karşılatırmak için
//            {
//                if (numbers[fflai2] > numbers[fflai3]) //*
//                {
//                    sortedNumbers.Add(numbers[fflai2]);

//                }
//            }
//        }
//    }   
//}

//for(int fflai4 = 0; 0<numbers.Count; fflai4++)
//{
//    Console.Write(sortedNumbers[fflai4]);
//}
